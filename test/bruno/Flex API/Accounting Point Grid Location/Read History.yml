info:
  name: Read History
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/accounting_point_grid_location_history/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
