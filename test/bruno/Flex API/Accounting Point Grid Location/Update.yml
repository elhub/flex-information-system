info:
  name: Update
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/accounting_point_grid_location/1"
  body:
    type: json
    data: |-
      {
        "additional_information": "Some additional information"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
