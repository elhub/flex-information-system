info:
  name: List History
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/accounting_point_grid_location_history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
