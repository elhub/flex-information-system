info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/accounting_point_grid_location"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
