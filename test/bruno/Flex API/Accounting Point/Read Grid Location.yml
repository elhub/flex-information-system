info:
  name: Read Grid Location
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/accounting_point/1/grid_location"
  auth:
    type: bearer
    token: <token>

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
