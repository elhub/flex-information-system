info:
  name: Lookup
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/controllable_unit/lookup"
  body:
    type: json
    data: |-
      {
        "end_user": "13370000001",
        "accounting_point": "133700000000010007"
      }
  auth:
    type: bearer
    token: <token>

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
